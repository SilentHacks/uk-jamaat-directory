# Optional override for host-level nginx (see deploy/nginx/directory.conf).
# Publishes the API on localhost only so nginx can reach it.
#
# Usage:
#   docker compose -f docker-compose.vps.yml -f docker-compose.vps.nginx.yml up -d

services:
  api:
    ports:
      - "127.0.0.1:8000:8000"
