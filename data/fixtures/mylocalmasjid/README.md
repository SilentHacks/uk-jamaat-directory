# MyLocalMasjid synthetic fixtures

These files are **hand-written synthetic examples** for adapter and import tests. They do not contain real MyLocalMasjid data and must not be replaced with restricted partner dumps.

Use `uk-jamaat-directory import-mlm --input data/fixtures/mylocalmasjid/sample_export.json` against a local database for manual smoke checks.
